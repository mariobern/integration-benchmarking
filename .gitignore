# Credentials - NEVER commit
config.yaml

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
.env

# Output files
*.csv
!price_id_list.csv
quick_benchmark_results.csv
backups/pre-perf-optimization

# Benchmark availability - track summary but ignore daily instrument lists
!benchmark_availability/SUMMARY.md
!benchmark_availability/history.csv
benchmark_availability/instruments_*.csv

# ISIN overrides (authoritative, tracked)
!.isin_overrides/isin_overrides.csv

# Cache directories
.isin_cache/
.nasdaq_cache/

# Local working copy of after.json (generated per governance proposal)
after.json

# Local governance-proposal snapshots (before.json / after.json under staging/)
staging/

# Local config working copies / backups (after_1.json, lazer.json, *.bak)
after_*.json
lazer*.json
*.json.bak

# Local DQ reports output
dq_reports/

# DQ summary spreadsheet outputs (dq_summary_*.xlsx, dq_hk_*.xlsx, hk_*.xlsx)
*.xlsx

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
